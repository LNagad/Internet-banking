@using Core.Application.Dtos.Pagos;
@{
    ViewData["Title"] = "No debe ninguna tarjeta!";
}

@model PagoTarjetaResponse
<div class="d-flex flex-column">

    <div class="card">
        <div class="card-body">
            <div class="alert alert-primary alert-dismissible fade show" role="alert">
                <h2>@Model.Error </h2>
                <button class="btn btn-close" data-bs-dismiss="alert" arial-label="Close"></button>
            </div>
        </div>
    </div>

    <a asp-controller="Home" asp-action="Index" class="btn btn-info">Volver atras</a>
</div>
